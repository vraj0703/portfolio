User-agent: *
Allow: /

# The plain-text version of the site, and the only page on it a crawler can
# read: the gallery is drawn on a canvas, so the root document has a title
# and nothing else. On the previous site this was disallowed as duplicate
# content, because the React page carried the same words in its own markup.
# It no longer does, so this is now the only thing worth indexing.
Allow: /static/

# www, not the bare apex. vishalraj.space answers 525 — its certificate is
# not resolving at the edge — so every apex URL here would point a crawler at
# an SSL error. The previous site's files all used the apex; that was copied
# forward without checking that it still served, and it does not.
Sitemap: https://www.vishalraj.space/sitemap.xml
